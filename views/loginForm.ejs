<div class="container auto text-center">
    <div class="row justify-content-center">
        <div class="login-form">
            <form action="" method="post">
    
                <div class="form-row justify-content-center">
                  <div class="form-group col-6">
                    <input type="text" class="form-control" name="name" id="name" placeholder="Usuario" required="required">
                  </div>
                  <div class="form-group col-6">
                    <input type="password" class="form-control" name="password" id="password" placeholder="Contraseña" required="required" minlength="8">
                  </div>

            
                  <div class="row justify-content-center">
                    <div class="col-md-10 col-lg-10 text-center">
                     <% if (typeof errors != 'undefined') { %>
                        <p style="color:red">ERROR</p>
                        <ul>
                           <% for (let i =0; i < errors.length; i++){ %>
                              <li>
                                 <%=errors[i].msg%>
                              </li>
                           <% } %>
                        </ul>
                     <% } %>
                  </div>
            
            
                  <div class="form-group button col-12">
                    <button class="myButton" type="submit" formaction="">Entrar</button>
                 </div>
              </div>
              </form>
              </div>
              </div>
              </div>
